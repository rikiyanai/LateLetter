# Garden Runtime Parity

Verified 2026-07-21 against the same synthetic real sealed bundle generated by
`demo_author.py` with the documented `biscuit` passphrase. “Full” means the
recipient-visible contract exists in both channels; it does not mean identical
rendering or final human acceptance.

| Feature | Terminal garden | HTML garden | Parity | Evidence / remaining work |
|---|---|---|---|---|
| Canonical bundle load | Reads and validates `.lateletter` JSON | File drop, demo fetch, or public-letter URL | Full | Same generated bundle loaded in Python and through the local HTML URL path. |
| Checksum gate | Blocks damaged bundles before recipient flow | Blocks unlock/archive when checksum fails | Full in code; negative QA pending | Valid-bundle positive path passed. A deliberately corrupted bundle still needs human negative-path QA in the browser matrix. |
| HMAC passphrase gate | Canonical `verify_bundle_hmac()` | WebCrypto HMAC before delivery disclosure | Full | Correct passphrase accepted and wrong passphrase rejected by terminal regression test; exact bundle unlocked in HTML. |
| Real message decryption | PBKDF2-SHA256 + AES-256-GCM via `open_message()` | Matching WebCrypto PBKDF2/AES-GCM | Full | Exact private label and body recovered in both implementations. |
| Real gift-sentiment decryption | `open_gift_sentiment()` after auth | Matching per-gift WebCrypto decrypt | Full | Terminal regression test recovers exact sentiment; HTML decrypt path uses the same authenticated bundle parameters. |
| Due-date delivery gate | Due/unread messages computed only after auth | Due/unread messages computed only after auth | Full | No dated list or delivery count is disclosed before HMAC success. |
| Letter archive and reread | Read/unread/future rows plus memories | Read/unread/future inbox plus memories | Full | Exact demo showed dated rows and opened the due letter in HTML; terminal helpers and recipient tests cover row state. |
| Read receipts | JSON under recipient-private storage, keyed by bundle/message | IndexedDB with localStorage fallback, same keys | Full contract | Persistence backend intentionally differs by platform. Browser-storage unavailable warning remains HTML-specific. |
| Deterministic garden and seasons | Bundle seed plus spring/summer/autumn/winter renderer | Bundle seed plus matching four-season renderer | Full contract | Rendering implementations are independent, so glyph-for-glyph identity is not expected. |
| Weather and particles | Rain, snow, leaves, splashes, clouds, lightning, accumulation | Rain, snow, leaves, resting leaves, accumulation | Partial | HTML still lacks the terminal renderer's full clouds/lightning/splash set. This is the clearest remaining visual-feature gap. |
| Ambient creatures | Birds, butterflies, and fireflies | Birds, butterflies, and fireflies | Full contract | Timing and paths are renderer-specific and still need comparative visual review. |
| First-run welcome | Timed “planted for you” message | Timed first-run banner | Full | Both use author name and persist first-run state locally. |
| Triggered items and memories | Date, cumulative-visit, and post-letter triggers; examine/archive overlays | Same trigger types; garden/archive overlays | Full | Real sentiment placeholders are removed in both channels. |
| Animal trust | Four tiers, cumulative feeding, absence traces, persistent state | Same thresholds, feeding, absence traces, persistent state | Full contract | Both support cat, bird, rabbit, and turtle art/delivery states. Comparative emotional/visual QA remains Part D work. |
| Delivery animation | Letter-bird, replaced by bonded animal | Letter-bird, replaced by bonded animal | Full contract | HTML exact-bundle run visibly reached its delivery animation and real reading screen. |
| Post-completion memorial | Memorial flower, permanent bonded animal/bird, all gifts | Corresponding post-completion state and permanent companion | Full contract | Still awaiting the planned human emotional-arc sign-off. |
| Save letter as text | `p` writes a plaintext file to Desktop | `[save]` triggers a browser download | Full outcome | Platform-native mechanism intentionally differs. |
| Published content | Opens whichever local bundle the recipient supplies | Live site currently offers synthetic demo copy | Intentional difference | The live personal letter remains unpublished because the former plaintext and passphrase were compromised. New personal copy and a new passphrase require explicit approval; this is not a runtime defect. |

The delivery/security rows are now at the same functional state. The remaining
parity work is visual breadth and human QA, principally HTML weather effects,
corrupted-file browser observation, the cross-browser matrix, and the Part D
emotional-arc review.
