# Garden multimodal QA — 2026-07-21

Scope: the only local worktree, normal sealed synthetic v2 bundle, public demo
passphrase `garden-biscuit-2026`. This is implementation evidence; it is not
the human sign-off required by §7.8.13 Gates 3 and 14.

## Bundle boundary

- `sealed_demo.lateletter` and `public_letters/to-a-friend.lateletter` were
  written through the canonical writer from an ignored synthetic source.
- Both pass checksum validation, v2 HMAC authentication, bounded PBKDF2,
  AES-GCM message decryption, encrypted Garden-program decryption, and strict
  program parsing.
- The exact decrypted content is fictional sample copy. No compromised Chloe
  copy or passphrase was inspected, reused, or published.

## Terminal production-path observation

The real curses recipient ran at 80×24. Separate isolated regressions exercised
wrong-pass and materialization-failure persistence boundaries byte-for-byte.

1. Before authentication it rendered only the fixed generic, nonpersistent
   preview Garden.
2. A wrong passphrase was rejected.
3. The correct passphrase unlocked the normal sealed bundle and swapped in the
   persistent authenticated world.
4. The terminal displayed the exact decrypted label `A demo letter` and the
   exact fictional body beginning `Dear friend,` and ending `Demo Author`.
5. Returning from the letter materialized the authored plant/fixture/animal/
   keepsake program and the lasting memorial state.
6. The explicit `d` control and idle loop advanced the same deterministic
   living-world clock; pause prevents advancement and reopening does not count
   already observed live time twice.
7. Object/action navigation, care, feed/play, journal, inventory, placement,
   movement/rotation, camera pan, and undo are routed through canonical
   commands.
8. A persisted nine-row journal/inventory view rendered in the real terminal;
   arrow scrolling exposes the complete bounded content rather than three rows.
9. The final replay also exercised a PTY that rejects cursor-visibility control;
   startup continued, wrong/correct-pass behavior remained intact, the exact
   letter and journal rendered, and curses restored the terminal on exit.

Live proof initially exposed a production-only failure: semantic placement
hints (`near_tallest_tree`, `near_bench`, `by_edge`) were rejected by the new
strict validator after authentication. The fix gives those documented hints
deterministic relative placement, runs every candidate through occupancy,
bounds, footprint, and reachability checks, rejects unknown hints, and now
matches browser materialization bytes. The real terminal flow was rerun after
the fix and passed.

## HTML production-path observation

The real viewer was served from localhost and operated through the in-app
browser.

1. The normal sealed demo loaded; wrong passphrase produced `incorrect
   passphrase, or file modified`.
2. The correct passphrase unlocked the bundle.
3. On the first click of the due date, the canonical letter-bird delivery
   completed and the exact decrypted letter appeared. The former undefined
   delivery-frame exception did not recur, and the read receipt is now written
   only after delivery and display.
4. The authenticated world exposed 51 canonical objects after the letter arc:
   5 animals, 9 collectibles, 23 fixtures, and 14 plants, including Clover,
   the demo keepsake, authored bench, and grown rose.
5. `dwell 30 seconds` visibly reported `The living world changed`; Clover's
   accessible description exposed species, bond tier, current intent,
   personality influence, and memory count.
6. Placement uses visible kind/catalog/x/y controls; actions expose move,
   rotate, collect, care, feed/play, journal, pan, pause, and undo.
7. At 375×812, the controls, sky consent panel, semantic object list, and
   responsive Garden remained visible without a fixed-cell hit-test owner.
8. Selecting the coarse Tokyo region changed the renderer label to `your local
   sky`; `forget sky location` immediately restored `storybook sky`.
9. Authentication opened persistence transactionally: program parsing and
   materialization complete before the first authenticated state commit.
10. A real history navigation away/back purged the decrypted body, label,
    program, sentiments, authenticated runtime, and form state; the restored
    page showed the fixed generic preview and required `open letters` again.
11. Recipient receipts and worlds use an authentication-derived namespace;
    separately sealed bundles with one public ID cannot see each other's state,
    while a legitimate APPEND/reseal retains the original namespace.
12. The final integrated replay repeated wrong-pass rejection, exact letter
    decryption, and history away/back after auth-epoch cancellation landed. The
    restored page contained 47 generic preview objects, no letter body/label,
    and no authenticated authored objects.

## Automated evidence

- Full Python suite: 592 passed after the integrated lanes stabilized.
- Browser/Node Garden suite: 52 passed.
- Production sealed-demo open/materialization was additionally exercised
  directly after the full suite.
- Coverage includes exact Python/JS world bytes, evaluator traces, semantic
  inputs, 100 topology seeds, 1,000 layouts, all connected masks and fixture
  footprints, responsive measured hit testing, four-species/tier presentation,
  deterministic dwell, DST/recurrence/missed summaries, 12 sky vectors,
  rollback, absence, ethics, transactional auth rollback, secret-bound
  receipt/world isolation, bfcache purge, bounded command/trace/undo histories,
  bounded visit/offline/recurrence ledgers, cross-system fixture effects,
  seven-stage plant interpolation, memory/weather/season animal decisions and
  locomotion, interruption-before-choreography parity, five connected families
  across all 16 masks, strict unknown-field rejection at nine shared bundle
  boundaries, auth-epoch runtime cancellation, transitive Pages dependency
  closure, and pre-auth persistence isolation.

## Installed-release evidence

- A clean Python 3.12 environment installed the project and all declared
  dependencies from `pyproject.toml`.
- Its wheel contained both canonical question banks plus the atlas, 24-star
  catalog, and astronomy provenance JSON.
- The installed package completed the scripted accessible Chloe-named author
  flow through questions, reviewed draft, timeline, canonical v2 sealing, and
  encrypted export without importing from the source checkout.
- The deployment verifier loads installed resources and authenticates,
  checksums, decrypts, and validates the tracked public safe bundle before
  Pages upload.
- A second clean wheel/virtual-environment replay after the final clearance
  fixes repeated all five installed-resource checks, public-bundle
  authentication/program validation, static-site preparation, and verify-only
  dependency closure.

## Remaining release evidence

Reconciled 2026-08-13 against the recorded operator decisions of 2026-08-10
through 2026-08-12 (FAILURE_LOG.md: batch operator decision 2026-08-11;
Wayfinder refinement on independent acceptance lanes 2026-08-11; product E2E
successor 2026-08-12 on commit cfabe67; gesture-ownership repair commit
bdab368). No gate is promoted beyond its named evidence; each bullet states
what its original wording still requires today.

- Human observation for standalone glance/tend/dwell value and the whole
  emotional arc (Gates 3 and 14). STILL OPEN — this is the pending operator
  visual/interaction acceptance.
- Remainder of Gate 2, as superseded: terminal traversal was WITHDRAWN by the
  2026-08-11 operator decision, and the accepted default composition contains
  no animal or collectible, so feed/play/collect actions are not in the
  accepted scenario. Mouse, keyboard and native CDP touch traversal of every
  action the accepted composition exposes is machine-executed (receipts of
  2026-08-12 on cfabe67 and 2026-08-13 on bdab368). Physical-handset touch
  remains OPEN, named fog.
- Supported browser atlas screenshot matrix (Gate 6). STILL OPEN; the
  terminal half is withdrawn with terminal parity.
- Reference desktop/mobile frame-time p95 evidence (Gate 10). STILL OPEN.
- Gate 12, as superseded: VoiceOver/NVDA are EXCLUDED by explicit operator
  direction (2026-08-11). 200% zoom was executed via CDP page scale 2 at the
  authoritative desktop layout (2026-08-12 on cfabe67; the 320-layout
  diagnostic is rejected as zoom evidence). No-color evidence remains OPEN.
- Post-push Pages deployment and published sealed-bundle replay (Gate 1).
