# Garden multimodal QA — 2026-07-21

Scope: local `master`, normal sealed synthetic bundle v2, passcode `garden`.
This is implementation evidence, not the human emotional-acceptance sign-off
required by §7.8.13 gates 3 and 14.

## Bundle boundary

- Regenerated `sealed_demo.lateletter` and
  `public_letters/to-a-friend.lateletter` through `make_letter.py`.
- Both artifacts pass canonical checksum validation, HMAC authentication,
  AES-GCM message decryption, encrypted Garden-program decryption, and strict
  program parsing.
- The encrypted program contains one animal, four authored entities, and five
  events: fixture reveal, letter-gated rabbit arrival, third-return plant
  growth, a normal-flow collectible reveal, and a bonded autumn gift.

## Terminal observation

The real curses recipient was launched in an isolated recipient-state
directory at 80×24. The following visible flow passed:

1. Open passphrase gate and enter `garden`.
2. Read the exact decrypted “A demo letter” label and fictional body.
3. Return to the Garden and observe canonical trace growth from authored
   fixture/narrative/rabbit/keepsake effects.
4. Select an object, open its actions, feed an animal, pause motion, and open a
   three-entry journal.
5. Place a bench and undo it through normal terminal commands.
6. Quit normally with exit status 0.

## HTML observation

The real viewer was served over localhost and operated through the in-app
browser:

1. Load `[get passcode-locked demo]`, open letters, enter `garden`, and unlock.
2. Open the due date, observe the delivery animation, and read the exact
   decrypted label/body.
3. Return to the Garden. The scene summary changed from 4 animals / 8
   collectibles / 14 fixtures to 5 animals / 9 collectibles / 15 fixtures,
   and the object list exposed the authored rabbit, bench, and demo-seed
   keepsake.
4. Select and collect the authored keepsake. Inventory changed from 0 to 1;
   the journal displayed the authored bench, Clover, and seed memories.
5. Pause motion and confirm the semantic summary reports “Motion paused.”
6. Enter the standalone Garden with no bundle; tend an oak, verify its journal
   observation, place a plant, undo it, and pan the camera from 0,0 to 1,0.
7. At a 320×812 viewport, the document had no horizontal overflow. All 49
   visible Garden controls were inside the viewport and measured at least
   44 CSS px high (minimum width 55 px).
8. Browser console errors/warnings: none during the run.

## Automated evidence

- Full Python suite: 424 passed.
- Browser Node contracts: 13 passed.
- Acceptance suite covers a real sealed v2 program, exact Python/JS authored
  world bytes/trace/receipts, 100 topology seeds, 1,000 layouts, all connected
  masks, temporal/DST/rollback behavior, replay, absence, static accessibility,
  and the explicit gate matrix.

## Open observation gates

- Direct human sign-off for glance/tend/dwell value and the complete emotional
  arc.
- VoiceOver and NVDA runs, no-color use, and 200% zoom completion.
- Browser/terminal atlas screenshot matrix.
- Ten-minute pan, browser p95, mobile p95, and terminal partial-diff evidence.
- Twelve trusted sky fixtures and live opt-in/denial/privacy browser checks.
- Complete four-species × four-tier behavior observation and memory-driven
  choice evidence.
